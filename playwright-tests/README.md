# Playwright Test

Automated test using Playwright.

## Test Description

The test opens https://playwright.dev/ and verifies that the page title contains the word "Playwright".

## Browsers

The test runs on:

- Chromium
- Firefox

## Run tests

Install dependencies:

npm install

Install browsers:

npx playwright install

Run tests:

npx playwright test
